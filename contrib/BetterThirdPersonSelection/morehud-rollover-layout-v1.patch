diff --git a/src/API/Compatibility.cpp b/src/API/Compatibility.cpp
index c11c833..9df78b5 100644
--- a/src/API/Compatibility.cpp
+++ b/src/API/Compatibility.cpp
@@ -5,6 +5,7 @@
 
 bool Compatibility::MoreHUD::IsInstalled = false;
 bool Compatibility::MoreHUD::IsEnabled = false;
+bool Compatibility::MoreHUD::SupportsRolloverLayout = false;
 
 bool Compatibility::QuickLootRE::IsInstalled = false;
 
@@ -37,10 +38,22 @@ bool Compatibility::MoreHUD::CheckModuleLoaded()
 	RE::GFxValue moreHudContainer;
 	hud->uiMovie->GetVariable(&moreHudContainer, "AHZWidgetContainer");
 
-	IsInstalled = !moreHudContainer.IsNull() && moreHudContainer.IsObject();
+	RE::GFxValue moreHudWidget;
+	IsInstalled = !moreHudContainer.IsNull() && moreHudContainer.IsObject() &&
+		moreHudContainer.GetMember("AHZWidget", &moreHudWidget) && moreHudWidget.IsObject();
+	SupportsRolloverLayout = false;
+	if (IsInstalled)
+	{
+		RE::GFxValue interopVersion;
+		if (moreHudWidget.Invoke("GetExternalRolloverLayoutVersion", &interopVersion, nullptr, 0) &&
+			interopVersion.IsNumber() && interopVersion.GetNumber() >= 1.0)
+		{
+			SupportsRolloverLayout = true;
+		}
+	}
 
 	if (IsInstalled)
-		logger::info("BTPS: compat - found MoreHUD");
+		logger::info("BTPS: compat - found MoreHUD{}", SupportsRolloverLayout ? " rollover-layout API v1" : "");
 
 	IsEnabled = IsInstalled && Settings::AdjustMoreHudWidgets;
 
diff --git a/src/API/Compatibility.h b/src/API/Compatibility.h
index a1ee15a..1e2ae26 100644
--- a/src/API/Compatibility.h
+++ b/src/API/Compatibility.h
@@ -56,6 +56,7 @@ public:
 	public:
 		static bool IsInstalled;
 		static bool IsEnabled;
+		static bool SupportsRolloverLayout;
 
 		static bool CheckModuleLoaded();
 
diff --git a/src/UI/SelectionWidget.cpp b/src/UI/SelectionWidget.cpp
index dabcf93..d9d59f2 100644
--- a/src/UI/SelectionWidget.cpp
+++ b/src/UI/SelectionWidget.cpp
@@ -17,6 +17,7 @@ glm::vec2 SelectionWidget::WidgetSize;
 bool SelectionWidget::UIValuesCached = false;
 bool SelectionWidget::ModulesLoadedChecked = false;
 bool SelectionWidget::OrigUIValuesSet = false;
+static bool MoreHUDRolloverLayoutActive = false;
 
 double SelectionWidget::FadeProgress = 0.0;
 EUIFadeMode SelectionWidget::FadeMode = NONE;
@@ -276,6 +277,14 @@ bool SelectionWidget::CacheGFxObjects()
 		CachedAhzWidget = GetGFxMember(CachedWidgetCont, "AHZWidget");
 		if (CachedAhzWidget.IsNull() || !CachedAhzWidget.IsObject()) return LogCacheErrorFail("MoreHUD - AHZWidgetContainer");
 
+		// moreHUD v1 owns the rollover text, image substitutions, reskinned icon
+		// clips, and side widget. BTPS only submits its normalized transform.
+		if (Compatibility::MoreHUD::SupportsRolloverLayout)
+		{
+			UIValuesCached = true;
+			return true;
+		}
+
 		CachedMoreHudContent = GetGFxMember(CachedAhzWidget, "content");
 		if (CachedMoreHudContent.IsNull() || !CachedMoreHudContent.IsObject()) return LogCacheErrorFail("MoreHUD - content");
 
@@ -327,7 +336,16 @@ void SelectionWidget::Update()
 	UpdateWidgetPos();
 
 	if (Compatibility::MoreHUD::IsEnabled)
-		UpdateMoreHUDWidget();
+	{
+		if (Compatibility::MoreHUD::SupportsRolloverLayout)
+			UpdateMoreHUDRolloverLayout();
+		else
+			UpdateMoreHUDWidget();
+	}
+	else if (MoreHUDRolloverLayoutActive)
+	{
+		ClearMoreHUDRolloverLayout();
+	}
 
 	if (Compatibility::QuickLootIE::IsEnabled)
 		UpdateQuickLootWidget();
@@ -452,6 +470,8 @@ void SelectionWidget::UpdateWidgetText()
 void SelectionWidget::HideNativeHUD()
 {
 	std::lock_guard lock(m_hideHUD);
+	if (Compatibility::MoreHUD::SupportsRolloverLayout)
+		return;
 
 	auto ui = RE::UI::GetSingleton();
 	if (!ui)
@@ -586,6 +606,56 @@ static constexpr double MoreHUDIconMargin = 10.0;
 static constexpr double MoreHUDEffectsSizeFact = 1.25;
 static constexpr double InfoWidgetOffsetLeft = 25.0;
 
+void SelectionWidget::UpdateMoreHUDRolloverLayout()
+{
+	std::lock_guard lock(m_manipulateMoreHUD);
+
+	if (!CachedAhzWidget.IsObject() || !CachedWidgetMenu || !CachedWidgetMenu->uiMovie)
+		return;
+
+	auto rect = SelectionWidgetMenu::GetMenuRect(CachedWidgetMenu.get());
+	auto rectWidth = rect.right - rect.left;
+	auto rectHeight = rect.bottom - rect.top;
+	if (rectWidth <= 0.0f || rectHeight <= 0.0f)
+		return;
+
+	auto widgetPos = GetWidgetPos();
+	auto widgetSize = GetWidgetSize();
+	double scale = OrigMCSize.x != 0.0f ? widgetSize.x / OrigMCSize.x : widgetSize.x / 100.0f;
+	double alpha = GetWidgetCurrentAlpha() / 100.0;
+	bool visible = SelectionWidgetMenu::IsVisible && FadeMode != FADED;
+
+	RE::GFxValue args[5]{
+		(widgetPos.x - rect.left) / rectWidth,
+		(widgetPos.y - rect.top) / rectHeight,
+		scale,
+		alpha,
+		visible
+	};
+
+	RE::GFxValue result;
+	if (CachedAhzWidget.Invoke("SetExternalRolloverLayout", &result, args, 5))
+	{
+		MoreHUDRolloverLayoutActive = true;
+
+		// Retain BTPS's progress circle while moreHUD renders the authoritative
+		// rollover text, information, activation button, and icon substitutions.
+		CachedActivateText.SetMember("_visible", false);
+		CachedGrayBar.SetMember("_visible", false);
+		CachedInfoText.SetMember("_visible", false);
+		CachedActivateButton.SetMember("_visible", false);
+	}
+}
+
+void SelectionWidget::ClearMoreHUDRolloverLayout()
+{
+	std::lock_guard lock(m_manipulateMoreHUD);
+	if (CachedAhzWidget.IsObject())
+		CachedAhzWidget.Invoke("ClearExternalRolloverLayout");
+
+	MoreHUDRolloverLayoutActive = false;
+}
+
 void SelectionWidget::UpdateMoreHUDWidget()
 {
 	std::lock_guard lock(m_manipulateMoreHUD);
diff --git a/src/UI/SelectionWidget.h b/src/UI/SelectionWidget.h
index 6ff9466..1cc20e6 100644
--- a/src/UI/SelectionWidget.h
+++ b/src/UI/SelectionWidget.h
@@ -104,6 +104,8 @@ public:
 	static glm::vec2 ToGlobal(glm::vec2 posIn, RE::GFxValue element, RE::GPtr<RE::IMenu> menu);
 
 	static void UpdateMoreHUDWidget();
+	static void UpdateMoreHUDRolloverLayout();
+	static void ClearMoreHUDRolloverLayout();
 	static void SetMoreHUDWidgetVisibility(bool mode);
 
 	static void SetQuickLootWidgetVisibility(bool mode);
diff --git a/src/lib/FenixUtils.cpp b/src/lib/FenixUtils.cpp
index bb9416a..f5ab747 100644
--- a/src/lib/FenixUtils.cpp
+++ b/src/lib/FenixUtils.cpp
@@ -17,10 +17,10 @@ RE::bhkPickData Util::CrosshairRaycast(RE::Actor* caster, RE::NiPoint3 ray_start
 	pick_data.rayInput.from = ray_start * havokWorldScale;
 	pick_data.rayInput.to = ray_end * havokWorldScale;
 
-	uint32_t collisionFilterInfo = 0;
+	RE::CFilter collisionFilterInfo{};
 	caster->GetCollisionFilterInfo(collisionFilterInfo);
-	pick_data.rayInput.filterInfo = (static_cast<uint32_t>(collisionFilterInfo >> 16) << 16) |
-				                    static_cast<uint32_t>(RE::COL_LAYER::kItemPicker);
+	collisionFilterInfo.SetCollisionLayer(RE::COL_LAYER::kItemPick);
+	pick_data.rayInput.filterInfo = collisionFilterInfo;
 
 	caster->GetParentCell()->GetbhkWorld()->PickObject(pick_data);
 	return pick_data;
diff --git a/src/lib/Util.cpp b/src/lib/Util.cpp
index 4ef28bc..09c98ef 100644
--- a/src/lib/Util.cpp
+++ b/src/lib/Util.cpp
@@ -309,9 +309,9 @@ std::string Util::GetCollisionLayer(CollisionFocusObject clObject)
 				auto collidable = havokRigidBody->GetCollidable();
 				auto clFilterInfo = collidable->broadPhaseHandle.collisionFilterInfo;
 
-				auto clLayer = clFilterInfo & 0x7f;
+				auto clLayer = clFilterInfo.GetCollisionLayer();
 
-				return std::to_string(clLayer);
+				return std::to_string(static_cast<std::uint32_t>(clLayer));
 			}
 		}
 	}
